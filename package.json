{
  "type": "module",
  "scripts": {
    "build": "rm -rf dist/* && npx @11ty/eleventy",
    "dev": "rm -rf dist/* && npx @11ty/eleventy --serve"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2"
  },
  "devDependencies": {
    "prettier": "3.6.2"
  }
}
