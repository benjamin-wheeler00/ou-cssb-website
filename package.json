{
  "type": "module",
  "scripts": {
    "build": "rm -rf dist/* && npx @11ty/eleventy",
    "dev": "rm -rf dist/* && npx @11ty/eleventy --serve"
  },
  "dependencies": {
    "eleventy": "^1.0.7"
  },
  "devDependencies": {
    "prettier": "3.6.2"
  }
}
